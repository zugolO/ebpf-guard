# Базовый СОСТАВ типов детекта (rule_id), критерий 6 run-gate.sh.
#
# Зачем состав, а не число (волна 5.3, находка №2 замера №2). Порог «>= 43 типа»
# был снят с прогона №4 и стал недостижим после законного сужения правил: замер
# №2 дал 41 тип и FAIL, хотя ни один настоящий детект не пропал — часть типов
# перестала срабатывать потому, что была FP и её сузили намеренно. Одно число не
# отличает «детект просел» от «FP перестал считаться детектом», и разбирать это
# пришлось руками. Гейт теперь печатает diff: потеряно / добавлено.
#
# Основание: замер №2 (TIMESTAMP=20260806_154041), 1096 новых алертов за прогон,
# 41 уникальный rule_id. Список снят с него как есть — это фиксация факта, а не
# целевое состояние.
#
# Как обновлять. Diff — это не автоматический провал: «добавлено» после волны,
# которая заводит правила, ожидаемо; «потеряно» после волны, которая правило
# намеренно сузила или переименовала, — тоже. Провалом считается потеря, которую
# никто не объяснил. Обновлять файл ТОЛЬКО вместе с записью в plan.md о том,
# какая волна и почему изменила состав, иначе база теряет смысл основания.
#
# Известное расхождение к замеру №2.1: волна 5.1 раздвоила семь правил кластера
# на базовое и *_daemon. В этом списке уже есть sigma_utmp_wtmp_modified_daemon
# (правило существовало до 5.1), а остальные шесть записаны в базовых именах —
# после 5.1 в diff ожидаются добавления вида *_daemon при сохранении базовых.
# Это ожидаемое «добавлено», не потеря.
#
# Обновление 5.5b (находка №7 замера №2.1): критерий 6 напечатал FAIL на пяти
# типах при исправном детекте — все пять присутствуют в alerts_filtered_total
# замера №2.1, просто их не стало в alerts_total/store после 5.1a (фильтр info
# на входе идёт ниже IncidentTracker, но выше сериализации alerts_total/store).
# Удалены из базы как отдельные строки (не «переименованы» — гейт сравнивает
# rule_id по alerts_total, а не по alerts_filtered_total). Два разных случая,
# свёрнутых в один симптом:
#   - rootkit_pam_module_added, sigma_failed_login_syscall — базовые имена
#     кластера волны 5.1; сейчас на idle/этом прогоне срабатывает только
#     *_daemon-двойник (severity info). Базовое имя остаётся валидным
#     rule_id — сработает и попадёт в alerts_total, если тронет процесс
#     вне [sshd, cron] — просто не в этом прогоне.
#   - sigma_utmp_wtmp_modified_daemon — было в базе уже с суффиксом (родилось
#     _daemon в P1-6, до волны 5.1); волна 5.1 поменяла только severity
#     warning → info — другая запись, не переименование.
#   - c2_periodic_beacon_pattern, recon_sensitive_file_find — НЕ из кластера
#     волны 5.1, severity: info изначально; тот же фильтр 5.1a впервые их
#     коснулся на этом прогоне. Ничего не переименовано, не тронуто волной 5.1.
# Если любой из пяти снова понадобится в базе — то с тем же типом записи, что
# здесь: строка + причина в этом файле и в plan.md (5.5b), а не тихий возврат.
# Добавлено: owasp_log_tampering — новое срабатывание правила (замер №2.1),
# не регресс и не связано с находкой №7.
#
# ВНИМАНИЕ, находка №10 (замер №2.2, 2026-08-07). Гейт напечатал «потеряно 5
# типов»: exfil_large_http_post, netintr_http_on_non_web_port, owasp_log_tampering,
# sigma_kernel_version_read, sigma_log_deletion. ЭТО НЕ ПОТЕРЯ — все пять живы в
# alerts_total этого же прогона, а их счётчики в baseline-alerts и final-alerts
# СОВПАДАЮТ ДО ЕДИНИЦЫ (15/15, 15/15, 3/3, 14/14, 5/5). Причина: критерий 6 строит
# состав как дельту списков алертов ПО id, то есть видит только сработавшее внутри
# 7.79-минутного окна атаки. Правила с частотой 2-20 срабатываний в час попадают в
# такое окно случайно; эти пять отстрелялись в 11:07-11:08, а baseline снят 11:08:50.
#
# НЕ УДАЛЯТЬ эти пять строк. Удаление «по инструкции гейта» испортит основание:
# на следующем прогоне граница ляжет иначе и «потеряются» другие пять. Правится
# сам критерий — волна 5.6a: состав считается по дельте счётчиков alerts_total из
# двух снимков /metrics, а не по дельте списка алертов. База при этом не меняется.
#
# ОЖИДАЕМО на замере №2.3 (волна 5.6c, записано заранее — 2026-08-08).
# netintr_loopback_high_port, скорее всего, НЕ сработает и будет напечатан как
# потерянный тип. Это следствие законного сужения правила, а не регресс детекта:
# 5.6c добавила условие `dport gt 49152`, которое правило всегда обещало именем
# («high port»), но никогда не проверяло. На замере №2.2 оно дало 82 срабатывания,
# и все — на портах НИЖЕ порога: сосед netintr_high_port_established (тот же
# `dport gt 49152`, но без ограничения по адресу) за весь прогон не сработал ни
# разу, а любое loopback-соединение выше 49152 обязано было бы поднять и его.
# Атаки стенда бьют по $VPS_IP:3000, а транзит docker-proxy идёт на порт 3000 —
# ни один сценарий не создаёт loopback-соединения выше 49152.
#
# Строку НЕ УДАЛЯТЬ и по итогам №2.3: rule_id остаётся валидным и сработает,
# как только появится сценарий с настоящим локальным прокси/релеем на
# эфемерном порту — ровно то, что правило и должно ловить (тот же тип записи,
# что у rootkit_pam_module_added выше). Если нужно вернуть тип в наблюдаемые —
# это делается сценарием атаки (локальный релей на порту > 49152), а не
# откатом условия: откат вернёт 82 ложных алерта в час и false
# incident_confirmed_attack на простое.
#
# Обновление 5.9g (находка №33, замер №2.5). Подтверждённый прогоном 5.8 состав
# потерь — ровно семь сетевых типов: exfil_large_http_post,
# initial_package_postinstall_network, netintr_http_on_non_web_port,
# netintr_large_upload_port, netintr_long_dns_session, netintr_loopback_high_port,
# netintr_syn_scan_pattern. Причина у всех одна и та же, отдельно от
# фонового/редкого различия 5.8a: этому стенду не хватает СЦЕНАРИЯ трафика, а не
# частоты срабатывания — здесь нет ни второго узла для эксфильтрации/аплоада
# крупных тел, ни long-lived DNS-сессии, ни постинсталла пакета по сети, ни
# порта > 49152 для loopback/non-web/syn-scan шаблонов. Это не то же самое, что
# background-rules.txt (правила, которые срабатывают на простое и не успевают в
# окно атаки) — эти семь не срабатывают вообще ни на простое, ни под атакой,
# потому что нужного трафика на стенде просто нет.
#
# Все семь ОСТАЮТСЯ валидными rule_id в этом списке (не удаляются, как было
# сделано с пятью правилами в 5.5b выше) — правило сработает, как только
# появится сценарий с соответствующим трафиком, и это не регресс детекта.
# Явный список для критерия 6, чтобы потеря именно этих семи типов печаталась
# как наблюдение, а не проваливала гейт — заведён отдельно,
# intentional-loss.txt (та же папка). Обновлять оба файла синхронно: имя,
# добавленное в один без другого, либо ломает состав базы, либо перестаёт
# гаситься в критерии 6.
#
# Обновление 5.9.3b (замер №2.9.2, gate-2.9.2.txt, TIMESTAMP=20260819_201036).
# Критерий 6 напечатал «добавлено (+26)» и PASS с явным требованием обновить
# базу тем же коммитом — сделано здесь. Список взят как есть из gate-2.9.2.txt
# (37 -> 63 типов), не пересчитан заново: волна 5.9.3b/c, идущая следом, меняет
# УСЛОВИЯ части этих самых правил (parent_comm/comm/argN контекст —
# c2_ingress_piped_to_shell, cis_5_2_5_privilege_escalation,
# container_escape_chroot, initial_driveby_browser_download_exec,
# initial_email_link_download_exec, initial_office_macro_exec,
# owasp_web_shell_spawn, sigma_chmod_executable_tmp [не тронуто],
# sigma_setuid_syscall, sigma_shell_from_unexpected_parent,
# sigma_web_server_shell_spawn, web_sql_injection_command — см. plan.md
# 5.9.3b/c), поэтому чьё именно точное срабатывание на новом прогоне
# подтвердит эти 26 типов — вопрос замера №2.9.3, не этого коммита.

anomaly_detection
beacon_fixed_interval
c2_ingress_piped_to_shell
cis_5_2_5_privilege_escalation
container_escape_chroot
container_escape_host_mount
container_escape_init_proc
container_escape_module_access
cred_proc_maps_mass_read
dns_tunneling_long_domain
drift_new_exec_critical
drift_new_file_dir_sensitive
drift_new_library_in_system_dir
evasion_chmod_sensitive
evasion_hidden_elf_in_tmp
exfil_archive_to_network_pipe
exfil_dns_txt_long_label
exfil_large_http_post
exfil_repeated_outbound_to_same_ip
incident_confirmed_attack
initial_driveby_browser_download_exec
initial_email_link_download_exec
initial_office_macro_exec
initial_package_postinstall_network
integrity_kernel_module_from_user_path
lateral_tool_transfer_wget
mitre_sandbox_detect_proc_read
net_high_frequency_connections
net_portscan_indicator
netintr_dns_long_label
netintr_http_on_non_web_port
netintr_large_upload_port
netintr_long_dns_session
netintr_loopback_high_port
netintr_syn_scan_pattern
owasp_log_tampering
owasp_reverse_shell_pattern
owasp_web_shell_spawn
privesc_suid_suspicious_path
rootkit_bpf_map_create_suspicious
rootkit_bpf_prog_load_suspicious
rootkit_hidden_dir_dev
rootkit_kmod_from_tmp
sensitive_file_read
sigma_binary_in_tmp_executed
sigma_chmod_executable_tmp
sigma_cpu_info_access
sigma_kernel_version_read
sigma_log_deletion
sigma_memory_proc_dump
sigma_passwd_shadow_read
sigma_reverse_shell_named_pipe
sigma_sensitive_file_chmod
sigma_setuid_syscall
sigma_shell_from_unexpected_parent
sigma_web_server_shell_spawn
web_internal_recon
web_sql_injection_command
web_sql_injection_files
webshell_crontab_modification
webshell_dns_exfil_long_subdomain
webshell_script_write_via_web_process
webshell_ssrf_internal_network
