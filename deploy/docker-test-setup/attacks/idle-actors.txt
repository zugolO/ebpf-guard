# Реестр акторов (comm), увиденных за idle-час — 5.9.9.F.2d (находка №118).
#
# Зачем этот файл. Дельта ebpf_guard_incidents_total{verdict="attack"} за
# idle-час (секция 5.9.9.F.1d, находка №115) НЕ сопоставима между замерами
# по числу: №2.9.9.F дала 7 (утро 08:07-09:07 UTC), №2.9.9.F.1 — 2
# (ночь 23:56-00:56 UTC), и разбор показал, что расхождение объясняется
# сменой фонового актора часа суток (утро — landscape-sysin/MOTD при
# sshd-логине, ночь — logrotate/dpkg-db-backup по таймеру), а не регрессом
# детекта. Порог по числу поэтому НЕ назначается (запрет 5.9.6, критерий с
# неизвестным правильным ответом выносит вердикт по случайности).
#
# Вместо порога — величина, которая от часа запуска не зависит: критерий
# 5.9.9.F.1d в run-gate.sh падает не на числе алертов idle-часа, а на
# НОВОМ АКТОРЕ — comm, которого нет ни в одной строке этого файла.
#
# Формат (TAB-separated, 3 поля): <comm>\t<замер>:<алертов за idle-час>\t<периодичность>
# Проверка membership смотрит только на поле 1 (comm), независимо от
# замера в поле 2 — реестр не привязан к конкретному прогону, он про то,
# ЧТО вообще бывает фоном на этом стенде в течение суток.
#
# КАК ПОЛУЧЕН СПИСОК. Оба уже снятых idle-часа этой волны — оба окна
# суток, представленных в архивах на момент постановки 5.9.9.F.2 (утро и
# ночь), а не только последний замер:
#   collect-2.9.9.F   (2.9.9.F,   08:07-09:07 UTC, "утро"): idle/alerts-start.json -> alerts-end.json
#   collect-2.9.9.F.1 (2.9.9.F.1, 23:56-00:56 UTC, "ночь"): idle/alerts-start.json -> alerts-end.json
# Дельта посчитана по .id (новые алерты стора за idle-час), группировка по
# .comm. Порог назначается не раньше, чем два idle-часа сойдутся в одном
# окне суток (5.9.9.F.2d) — до этого момента реестр только ПОПОЛНЯЕТСЯ
# новыми, реально увиденными акторами, а не подгоняется под предсказание.

# --- утро (2.9.9.F, 08:07-09:07 UTC) — sshd-логин + MOTD-скрипты + landscape-sysinfo ---
landscape-sysin	2.9.9.F:61	по логину (landscape-sysinfo, вызывается MOTD-цепочкой при sshd-сессии)
systemd-logind	2.9.9.F:42	по логину (регистрация сессии)
sshd	2.9.9.F:17	по логину (сессии наблюдателя/крона)
python3	2.9.9.F:12	по логину (landscape-sysinfo, python3-реализация)
sh	2.9.9.F:11	по логину (обвязка MOTD-скриптов /etc/update-motd.d)
00-header	2.9.9.F:6	по логину (MOTD-скрипт 00-header)
91-release-upgr	2.9.9.F:6	по логину (MOTD-скрипт 91-release-upgrade)
awk	2.9.9.F:6	по логину (вызывается MOTD-скриптами)
bash	2.9.9.F:6	по логину (обвязка MOTD-скриптов)
dbus-daemon	2.9.9.F:6	системный демон (фон)
egrep	2.9.9.F:6	по логину (вызывается MOTD-скриптами)
grep	2.9.9.F:6	по логину (вызывается MOTD-скриптами)
env	2.9.9.F:4	по логину (вызывается MOTD-скриптами)
grafana	2.9.9.F:4	системный демон (фон)
snapd	2.9.9.F:4	системный демон (фон)
systemd	2.9.9.F:4	системный демон (фон)
(python3)	2.9.9.F:2	по логину (landscape-sysinfo, усечённое имя comm)
10-help-text	2.9.9.F:1	по логину (MOTD-скрипт 10-help-text)
50-landscape-sy	2.9.9.F:1	по логину (MOTD-скрипт 50-landscape-sysinfo)
85-fwupd	2.9.9.F:1	по логину (MOTD-скрипт 85-fwupd)
90-updates-avai	2.9.9.F:1	по логину (MOTD-скрипт 90-updates-available)
find	2.9.9.F:1	по логину (вызывается MOTD-скриптами)
prometheus	2.9.9.F:1	системный демон (фон)
tr	2.9.9.F:1	по логину (вызывается MOTD-скриптами)
who	2.9.9.F:1	по логину (вызывается MOTD-скриптами)

# --- ночь (2.9.9.F.1, 23:56-00:56 UTC) — logrotate/dpkg-db-backup по таймеру ---
(ogrotate)	2.9.9.F.1:13	по таймеру logrotate.timer (усечённое имя comm)
systemd	2.9.9.F.1:8	системный демон (фон)
logrotate	2.9.9.F.1:5	по таймеру logrotate.timer (ежедневно)
cmp	2.9.9.F.1:4	по таймеру dpkg-db-backup.timer (сверка дампа, ежедневно)
snapd	2.9.9.F.1:3	системный демон (фон)
grafana	2.9.9.F.1:2	системный демон (фон)
tar	2.9.9.F.1:2	по таймеру dpkg-db-backup.timer (архивация, ежедневно)
(b-backup)	2.9.9.F.1:1	по таймеру dpkg-db-backup.timer (усечённое имя comm)
dpkg-db-backup	2.9.9.F.1:1	по таймеру dpkg-db-backup.timer (ежедневно)
run-parts	2.9.9.F.1:1	по таймеру (обвязка /etc/cron.*/ через run-parts)

# --- 5.9.9.F.5i/№159 (замер №2.9.9.F.4, впервые наблюдено) — таймер Ubuntu
# MOTD-новостей, новое окно суток относительно предыдущих замеров. Запрет
# постановки №2.9.9.F.3 касался mandb/find/install (их здесь нет) —
# пополнение этой записью законно. Оговорка постановки 5.9.9.F.5i: сама эта
# запись НЕ закрывает величину 3 (idle-час, verdict="attack") — промоушен
# wget до incident_confirmed_attack на этом акторе остаётся FAIL до разбора
# по существу (netintr_syn_scan_pattern/lateral_tool_transfer_wget на
# штатном таймере, сходившем в сеть, — это класс «правило шире своего
# имени», отдельный от вопроса «новый ли это актор»).
run-parts	2.9.9.F.4:1	по таймеру (обвязка /etc/cron.*/50-motd-news через run-parts)
50-motd-news	2.9.9.F.4:1	по таймеру apt-news.timer/50-motd-news (Ubuntu MOTD news)
(otd-news)	2.9.9.F.4:1	по таймеру apt-news.timer/50-motd-news (усечённое имя comm)
wget	2.9.9.F.4:1	по таймеру 50-motd-news (загрузка apt-news JSON)
dpkg	2.9.9.F.4:1	по таймеру 50-motd-news (dpkg --print-architecture)
dpkg-query	2.9.9.F.4:1	по таймеру 50-motd-news (dpkg-query -W)
mktemp	2.9.9.F.4:1	по таймеру 50-motd-news (временный файл для загрузки)
uname	2.9.9.F.4:1	по таймеру 50-motd-news (uname -m)
sshd	2.9.9.F.1:1	по логину (сессии наблюдателя/крона)
libuv-worker	2.9.9.F.5:1	рабочий поток grafana (root_comm=containerd-shim); один алерт anomaly_detection warning, score 9, промоушен в suspicious — 5.9.9.F.6c, №172
fwupd	2.9.9.F.7:12	по таймеру fwupd-refresh.service (суточный, демон обновления прошивок); 5 critical — /dev/mem, /proc/kcore, /sys/module, DMI; промоушен в attack score=50.0 при пороге 50 (5.9.9.F.7e, №179)
(fwupd)	2.9.9.F.7:2	форкнутый ребёнок fwupd до execve (усечённое имя comm)
fwupdmgr	2.9.9.F.7:3	CLI, которым fwupd-refresh.service дёргает обновление метаданных (суточный таймер)
(fwupdmgr)	2.9.9.F.7:3	форкнутый ребёнок fwupdmgr до execve (усечённое имя comm)
#
# 6.0f (замер 01.09, окно 07:45-08:45 UTC): systemd-tmpfiles-clean.timer —
# суточный таймер, чей час впервые совпал с idle-часом замера. Это ЧЕТВЁРТЫЙ
# случай законного пополнения реестра новым окном суток (после 50-motd-news
# 5.9.9.F.4, libuv-worker 5.9.9.F.6c и fwupd 5.9.9.F.8a). Запрет постановки
# 5.9.9.F.3 сюда НЕ распространяется: он поимённо закрывает mandb/install/
# (find) — всплеск 11:57:39 замера №2.9.9.F.2, находка №133, — а не любого
# актора пакета systemd.
#
# ГРАНИЦА, как у fwupd (№179): реестр закрывает ГЕЙТ (состав акторов часа).
# Продуктовая сторона — critical sigma_memory_proc_dump на /proc/1/environ и
# два warning на том же файле, включая container_escape_init_proc из
# подмножества (б) волны 6 — реестром НЕ закрывается и закрываться не должна.
# Это отдельная открытая находка, правка правил здесь ЗАПРЕЩЕНА.
#
# pgrep в реестр НЕ дописан намеренно: его 596 алертов за этот час — не фон
# стенда, а осиротевшие поллеры измерителя (два цикла с `pgrep -f` в теле,
# 10 вызовов/мин, 6.5 ч). Источник снят, метрика вернулась 20/мин -> 0/мин.
# Дописывание pgrep узаконило бы загрязнение измерителем.
systemd-tmpfile	6.0f:6	по таймеру systemd-tmpfiles-clean.service (суточный, очистка /tmp); 1 critical — sigma_memory_proc_dump на /proc/1/environ, плюс container_escape_init_proc и mitre_sandbox_detect_proc_read на том же файле (6.0f, открытая находка)
(tmpfiles)	6.0f:1	форкнутый ребёнок systemd-tmpfiles до execve (усечённое имя comm); один алерт anomaly_detection warning
# ── Волна 6.2 (2026-09-03): акторы, принесённые k8s-нодой ──────────────────
# Стенд получил k3s v1.36.4+k3s1 (single-node). Пункт Д «Переноса в 6.1…6.4»
# требует пополнить реестр ДО прогона, а не после FAIL: ровно одним таким
# FAIL («новые акторы idle-часа вне реестра») закончился гейт замера №6.0d,
# и тогда речь шла о двух акторах.
#
# ЧЕМ ЭТИ СТРОКИ ОТЛИЧАЮТСЯ ОТ ОСТАЛЬНЫХ. Числа сняты НЕ за idle-час, а за
# семиминутное окно 18:53:34…19:00 UTC сразу после подъёма ноды и рестарта
# агента, — то есть это окно СТАРТА кластера (образы, iptables-правила, CNI),
# а не стационарный фон. Поле «замер» помечено 6.2-preflight именно поэтому:
# членство в реестре читается по полю 1, а число здесь — не предсказание
# idle-часа. Окончательные величины даст БАЗОВЫЙ прогон 6.2 (пункт А: первый
# прогон после подъёма ноды меряет обучение, а не базовую линию).
#
# ЧЕГО ЗДЕСЬ НЕТ НАМЕРЕННО. В том же окне видны kubectl/curl/jq/cat/date/
# systemctl/journalctl/sh — это МОЯ интерактивная сессия подъёма ноды, а не
# фон стенда. Вносить их значило бы повторить дефект замера 6.0.F, где
# осиротевшие поллеры наблюдателя дали 1195 совпадений/ч и новый актор
# (память ebpf-guard-measurement-hygiene, п.5). Они появятся в реестре, только
# если их увидит чистый idle-час без наблюдателя.
k3s-server	6.2-preflight:497	демон k3s (control-plane + kubelet + встроенный containerd в одном процессе)
containerd	6.2-preflight:70	containerd ноды (у k3s свой, /run/k3s/containerd/containerd.sock)
containerd-shim	6.2-preflight:16	shim пода (усечение containerd-shim-runc-v2)
runc	6.2-preflight:9	создание контейнера пода
runc:[1:CHILD]	6.2-preflight:13	промежуточный процесс runc при создании контейнера
runc:[2:INIT]	6.2-preflight:23	init контейнера; поднимает container-escape/rootkit/cis — до-6.1 источник, критерий 6.2.6
coredns	6.2-preflight:202	DNS кластера (под kube-system); сетевые правила, см. критерий 6.2.3
local-path-prov	6.2-preflight:11	local-path-provisioner (под kube-system, усечённое имя comm)
iptables	6.2-preflight:126	программирование правил сервисов/CNI ноды
ip6tables	6.2-preflight:21	то же для IPv6
iptables-save	6.2-preflight:1	снятие текущего набора правил перед правкой
iptables-restor	6.2-preflight:1	применение набора правил (усечённое имя comm)
flannel	6.2-preflight:1	CNI k3s
bridge	6.2-preflight:6	CNI-плагин bridge (setup сети пода)
loopback	6.2-preflight:4	CNI-плагин loopback (setup сети пода)
mount	6.2-preflight:4	монтирование томов пода kubelet'ом
