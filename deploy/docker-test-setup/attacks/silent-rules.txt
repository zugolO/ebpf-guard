# Реестр правил с нулевым ebpf_guard_alerts_total за ВЕСЬ аптайм агента
# (не за окно прогона — за жизнь процесса), волна 5.9.4h, находка №59.
#
# Отличие от intentional-loss.txt/background-rules.txt: те списки — про
# критерий 6 (состав детекта в ОДНОМ прогоне, окно атаки ± idle-час).
# Этот файл — про отдельный машинный гейт: любое правило, немое за весь
# аптайм, обязано быть здесь с категорией, иначе run-gate.sh проваливает
# его как категорию (в) «без объяснения» (см. run-gate.sh, секция
# "правила, немые за весь аптайм").
#
# ФОРМАТ: одна строка на правило — "<rule_id> <категория>", без хвостовых
# комментариев на той же строке (пояснение — отдельным блоком выше списка,
# как в intentional-loss.txt). Категории:
#   a — немо по конструкции (ловится UnreachableSyscallRules/
#       ContextEmptySyscallRules — недостижимый nr или условие без
#       контекста; см. соответствующий rules_coverage_test.go)
#   b — немо из-за среды: условие корректно и достижимо, но этот стенд не
#       воспроизводит сценарий, который бы его вызвал (ни на простое, ни
#       под атакой). Снимается волной 6 добавлением сценария, не правкой
#       правила.
# Категория (в) «без объяснения» в этом файле не имеет строк по
# построению — она и есть множество, не попавшее сюда.
#
# ─────────────────────────────────────────────────────────────────────────────
# Волна 5.9.4h (находка №59): три правила из таблицы находки №57, не
# сработавшие НИ РАЗУ ни на одном известном прогоне (№2.9.2, №2.9.3),
# в отличие от восьми соседей по той же таблице, у которых была история FP
# (см. intentional-loss.txt, блок 5.9.4f). Все три — event_type: file с
# условием на конкретные filename, синтаксически корректны и достижимы
# (FileaccessCollector трекает open(2) по умолчанию, internal/collector/
# fileaccess.go); ни один сценарий в run-all-attacks.sh не открывает эти
# пути:
#   sigma_cpu_info_access    — /proc/cpuinfo, /proc/meminfo, /proc/version,
#       /proc/sys/kernel/{hostname,osrelease,version}: разведка через
#       чтение системных файлов, у атаки на этом стенде нет шага, который
#       бы их открывал (собственные exceptions правила гасят чтения самого
#       ebpf-guard/pgrep).
#   sigma_kernel_version_read — /proc/version, /etc/*-release: OS
#       fingerprinting, тот же случай: нет attack-шага.
#   sigma_memory_proc_dump   — /proc/[pid]/{mem,maps,environ,cmdline}:
#       дамп памяти чужого процесса, нет attack-шага (кроме штатных
#       чтений systemd-journal, исключённых в самом правиле).
#
# web_sql_injection_files — четвёртое правило из находки №59, отдельное от
# трёх выше: единственный исторический экземпляр (2 алерта на
# systemd-logind, №2.9.2) был FP, убитым узким контекстом волны 5.9.3b
# (см. intentional-loss.txt, блок 5.9.4f) — искать в этом файле его
# больше нечему. Условие — SQL-паттерны в ИМЕНИ файла (union select, DROP
# TABLE и т.п. как подстрока filename); ни один сценарий SQLi в
# run-all-attacks.sh не создаёт файл с таким именем — инъекция идёт через
# сетевой запрос, а не через запись на диск. Категория та же: нет входных
# данных, не дефект правила.
#
# Волна 6 добавляет сценарий: reconnaissance-шаг (cat /proc/cpuinfo,
# /proc/version, /etc/os-release), чтение /proc/<pid>/mem инструментом
# вроде gdb/procdump, и webshell-загрузку с SQLi-паттерном в имени файла —
# после чего все четыре либо срабатывают и уходят из этого списка, либо
# остаются здесь по новой, более конкретной причине.
#
# Волна 5.9.7e (находка №81): mitre_software_enum_dpkg — отличается от
# четырёх правил выше тем, что за аптайм №2.9.5 сработало один раз
# (09:27:00, dpkg-query /var/lib/dpkg/status), а не нулём — но этот один раз
# был оператором, вручную вызвавшим dpkg-query на стенде вне какого-либо
# attack-шага (5.9.6f подняла его в detection-baseline.txt как «сработал»,
# не проверив источник; см. plan.md 5.9.7e). run-all-attacks.sh не вызывает
# dpkg-query/rpm ни в одном сценарии — на прогоне без ручного вмешательства
# оператора правило молчит весь аптайм, поэтому категория та же (b), что и
# у соседей: условие достижимо и корректно, сценария у стенда нет. Если
# правило снова сработает НЕ от dpkg-query оператора — это повод убрать его
# из этого списка, а не тихо перезаписать причину.
# Волна 5.9.9c (находка №101): container_escape_pivot_root — сосед
# container_escape_cap_sys_admin по rules/container-escape.yaml, тот же
# список исключений [runc, containerd-shim, containerd-shim-runc-v2,
# dockerd, conmon, crun], та же природа (немо не по конструкции, а из-за
# отсутствия сценария трафика на этом стенде). Отличие от cap_sys_admin —
# у cap_sys_admin нашёлся безопасный способ дать трафик (unshare -U true,
# действие alert, см. run_container_escape_positive_control); у
# pivot_root безопасного способа нет — вызов pivot_root(2) меняет корень
# самого вызывающего процесса, и это не откатить так же, как временный
# файл или символическую ссылку. container_escape_pivot_root не заведён в
# detection-baseline.txt (никогда не срабатывал ни на одном известном
# прогоне), поэтому машинный гейт 5.9.4h его не проверяет — строка ниже
# заведена постановкой 5.9.9c как запись факта, а не потому что 5.9.4h её
# потребовал. Снимается волной 6, если найдётся безопасный способ вызвать
# pivot_root(2) из-под харнесса.
# Волна 5.9.9.Fa (находка №107): sigma_memory_proc_dump СНЯТ из списка
# ниже. Его строка (b) была верна ровно до тех пор, пока у стенда не было
# ни одного шага, читающего /proc/<pid>/{mem,maps,environ,cmdline} чужого
# процесса, — блок 5.9.4h выше это прямо и говорит. Шаг появился:
# run_cred_proc_maps_positive_control (run-all-attacks.sh, 5.9.9.Fa) читает
# /proc/<pid>/maps чужого живого процесса восемь раз подряд под comm=
# credscrape, не попадающим ни под одно исключение правила (systemd-journal,
# ebpf-guard, pgrep+cmdline). Правило обязано на нём сработать, и оставлять
# строку (b) означало бы гасить именно тот сигнал, ради которого шаг заведён:
# устаревшая строка здесь делает гейт МЯГЧЕ, а не строже (в отличие от
# background-rules.txt, где устаревание строже — см. 5.9.9.Fe). Если
# sigma_memory_proc_dump всё-таки промолчит на №2.9.9.F — это находка о
# сломанном контроле, и её обязан показать FAIL категории (в), а не эта
# строка.
container_escape_pivot_root b
mitre_software_enum_dpkg b
sigma_cpu_info_access b
sigma_kernel_version_read b
web_sql_injection_files b

# Волна 5.9.9.F.2e (находка №122): 11 правил, которые агент сам называет
# при старте — "rules: syscall rules with no reachable nr in the kernel
# allowlist" (cmd/ebpf-guard/main.go) — event_type: syscall с условием на
# числовой nr вне cfg.BPF.KernelFilter.MonitoredSyscalls. Никогда не
# срабатывали ни на одном известном прогоне и не заведены в
# detection-baseline.txt вовсе (baseline — это то, что хоть раз сработало),
# поэтому $lost_types критерия 6 их не видит по построению: находка не в
# потере, а в отсутствии самой возможности потерять. Категория (а) «немо
# по конструкции» ровно по определению этой категории выше — это тот же
# UnreachableSyscallRules, что уже проверяет rules_coverage_test.go.
# Снимается волной 6, если новый источник событий (расширенный
# KernelFilter.MonitoredSyscalls) сделает nr достижимым.
mitre_arp_spoof_raw_socket a
mitre_sandbox_detect_cpuid a
ransomware_backup_delete a
ransomware_mass_rename a
rootkit_anonymous_exec_memory a
rootkit_kexec_load a
rootkit_userfaultfd_create a
sigma_mprotect_exec_heap a
sigma_prctl_dumpable a
sigma_seccomp_filter_install a
web_blind_sqli_heuristic a
