# Реестр обязательных файлов архива волны 6.4 (item 3 волны 6.5, находка №459).
#
# ЗАЧЕМ. №459: тождество бинаря писалось в каталог, который шаг 1 пайплайна
# сам же очищал, и в архив не попадало НИ РАЗУ — при этом лог утверждал
# «кладётся в $ART, значит попадает в архив». Точечный сторож на ОДИН файл
# заведён; предмет этого реестра — СПИСОК того, что архив ОБЯЗАН нести, и
# проверка СПИСКА на сборке (wave6.4-completeness-guard.sh --archive),
# а не по одному файлу после каждой потери.
#
# ФОРМАТ. Один путь ОТНОСИТЕЛЬНО КОРНЯ АРХИВА в строке. Строки, начинающиеся
# с `#`, и пустые строки игнорируются. Проверка: файл существует И непуст;
# каталог файлом не считается (у `-s` на каталоге размер > 0, это ловушка).
#
# ЧТО И ПОЧЕМУ ИМЕННО ЭТИ. Здесь только то, что пайплайн ОБЕЩАЕТ положить в
# архив и чья потеря иначе МОЛЧАЛИВА (копирование идёт с `2>/dev/null`):
#   - величины прогона и их реестр: emitted-labels, вердикты контролей;
#   - тождество бинаря: пара A/B метки 6.4.6 держится только на нём (№459);
#   - провенанс прибора: конфиг стенда, git-head, скрипты волны;
#   - состояние стенда: узлы/поды/версия;
#   - сам этот реестр (архив хранит список, по которому проверен).
#
# ЧЕГО ЗДЕСЬ НАМЕРЕННО НЕТ:
#   - run-6.4.log: он кладётся ЛИТЕРАЛЬНО последним действием (№241) уже
#     ПОСЛЕ этой проверки, чтобы архивная копия была полна; его непустоту
#     гарантирует сам факт дохождения пайплайна до строки проверки;
#   - файлы, зависящие от среды и чьё отсутствие пайплайн объявляет СЛОВОМ
#     (снимок на закрытии, journal-agent, noise-diag, tls-манифест): реестр
#     ловит МОЛЧАЛИВУЮ потерю, а не уже названную.
controls/artifacts/emitted-labels.txt
controls/artifacts/binary-identity.txt
controls/wave6.3-controls-verdicts.txt
git-head.txt
config-test.yaml
run-6.4-pipeline.sh
wave6.4-completeness-guard.sh
wave6.4-emitter-fixtures.sh
wave6.3-controls.sh
wave6.3-metrics-lib.sh
wave6.3.9f-item3-baseline-controls.sh
node/nodes.txt
node/pods.txt
node/version.json
wave6.5-archive-manifest.txt
wave6.5-item5-http-control.sh
