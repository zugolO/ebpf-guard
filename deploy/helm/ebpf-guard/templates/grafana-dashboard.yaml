{{- if .Values.grafana.dashboard.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ebpf-guard.fullname" . }}-grafana-dashboard
  namespace: {{ .Values.grafana.dashboard.namespace | default (include "ebpf-guard.namespace" .) }}
  labels:
    {{- include "ebpf-guard.labels" . | nindent 4 }}
    {{ .Values.grafana.dashboard.label }}: {{ .Values.grafana.dashboard.labelValue | quote }}
data:
  ebpf-guard-dashboard.json: |-
    {{- .Files.Get "dashboards/ebpf-guard-dashboard.json" | nindent 4 }}
{{- end }}
