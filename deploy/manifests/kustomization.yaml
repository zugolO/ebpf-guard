apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ebpf-guard

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - configmap.yaml
  - daemonset.yaml

commonLabels:
  app.kubernetes.io/name: ebpf-guard
  app.kubernetes.io/version: "0.10.0-alpha"
