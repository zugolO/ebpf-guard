# ebpf-guard v0.1 config snapshot — used by TestMigrationFromAllVersions.
# Fields here use the pre-v0.2.0 schema: profiler.ewma_weight and alerting.webhook_url.
config_version: "v0.1"

server:
  bind_address: ":9090"
  metrics_path: "/metrics"
  health_path: "/health"

bpf:
  map_sizes:
    events: 65536
    processes: 16384
    connections: 32768

rules:
  path: "/etc/ebpf-guard/rules.yaml"
  hot_reload: true
  rate_limit_alerts: true
  rate_limit_window: 60
  max_alerts_per_window: 10

profiler:
  enabled: true
  learning_period: 3600
  min_learning_samples: 100
  anomaly_threshold: 0.8
  ewma_weight: 0.3
  profile_ttl: 86400

alerting:
  enabled: false
  webhook_url: ""
  generator_url: "http://ebpf-guard:9090"
  batch_size: 100
  batch_timeout: 5

kubernetes:
  enabled: false
  kubeconfig_path: ""
  resync_period: 300
